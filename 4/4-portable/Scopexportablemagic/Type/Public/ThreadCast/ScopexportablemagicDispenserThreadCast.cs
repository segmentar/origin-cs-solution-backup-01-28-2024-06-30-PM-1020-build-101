using Core;

namespace Core
{
    using System;

    using System.Threading;

    public partial class Scopexportablemagic
    {
        public static Thread ScopexportablemagicThreadCastDispenser(Object reflect_OBJECT)
        {
            Thread threadResult = default;

            var result = (Thread)(reflect_OBJECT as Object);

            threadResult = result;

            return threadResult;
        }
    }
}
