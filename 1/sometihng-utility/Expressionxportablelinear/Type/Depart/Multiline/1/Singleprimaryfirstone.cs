using Core;

namespace Core
{
    using System;

    public partial class Expressionxportablelinear
    {
        public static void Singleprimaryfirstone()
        {
            var current = ExecuteLinkedList.First;

            while (true)
            {
                var reflect = (String)(current.Value as Object);

                var lower = reflect.ToLower();

                Boolean isEqualCheck, shouldBreakCheck;

                isEqualCheck = Object.Equals(Expressionxportablestoremarker.EntityCall, lower) is true;

                shouldBreakCheck = isEqualCheck is true;

                if (shouldBreakCheck is true)
                {
                    Doublesecondarysecondtwo();

                    break;
                }
                else
                    "false".ToString();

                current = current.Next;

                ParameterArrayList.Add(current.Value);

                continue;
            }

            return;
        }
    }
}
